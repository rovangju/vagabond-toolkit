#!/usr/bin/env bash

target="."

until [[ -e "$target/.git" ]]; do
    target="../$target"
    if [[ $( cd "$target" && pwd ) == "/" ]]; then
        >&2 echo "Failed to find .git"
        exit 1
    fi
done

ln -v -s -T "$target" base
